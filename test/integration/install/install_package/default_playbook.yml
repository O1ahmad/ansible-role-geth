---
- name: Default playbook for installing Geth via OS system package managers
  hosts: test-kitchen
  roles:
    - ansible-role-geth
  vars:
    geth_user: test
    install_type: package
    manage_service: false
